# herdr-automatic-rename overrides.

# Number tabs only. Ctrl+B 1-9 (switch_tab) is bound, so a tab's [N] is a live
# jump key; switch_workspace is not bound, so the same prefix on a space was
# decoration that only ate sidebar width. Setting this to 0 also strips the
# prefixes already on those rows at the next herdr event.
AUTO_INDEX_WORKSPACES=0
